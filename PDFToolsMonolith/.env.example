# Server
PORT=4000
NODE_ENV=development

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=pdf_tools_db
PGUSER=pdf_user
PGPASSWORD=please-set-strong-password
PGSSL=false

# Auth/session
JWT_SECRET=replace-with-strong-secret
SESSION_TTL_HOURS=24

# Storage
STORAGE_DIR=./storage
PUBLIC_DOWNLOAD_TTL_HOURS=24
MAX_UPLOAD_MB=50

# CORS / Frontend URL
CLIENT_URL=http://localhost:3000
